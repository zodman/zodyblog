
   <div class="right" >    <span class="a"  onclick="hideComments();"> Hide Comments </span></div>
<div class="comments">
 {% for c in comments %}
    <div class="each-comment">
        <a href="{{c.web}}">{{c.name}}</a> <small> {{c.getdate}}</small> say:
        <div class="comment-content">
            {{c.comment|striptags}}
        </div>
    </div>
 {% endfor %}
 <div>
